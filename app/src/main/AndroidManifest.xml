<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.unipu.fipu.pulabus_v2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ostalo.Ulaz">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ostalo.Pocetna"></activity>
        <activity android:name=".omiljeno_button.Omiljeno"></activity>
        <activity android:name=".omiljeno_button.OmiljenoLinije"></activity>
        <activity android:name=".omiljeno_button.OmiljenoStanice"></activity>
        <activity android:name=".vrijeme_button.Vrijeme"></activity>
        <activity android:name=".vrijeme_button.VrijemeLinije"></activity>
        <activity android:name=".linije_button.LinijePolasci"></activity>

    </application>

</manifest>